# NextAuth
NEXTAUTH_SECRET="change-moi-en-production"
NEXTAUTH_URL="http://localhost:3000"

# Keycloak (authentification — voir keycloak/realm-export.json).
# keycloak.local doit être résolu en 127.0.0.1 côté hôte Windows
# (C:\Windows\System32\drivers\etc\hosts) — voir extra_hosts dans
# docker-compose.yml pour la résolution côté conteneur.
KEYCLOAK_CLIENT_ID="educi-frontend"
KEYCLOAK_CLIENT_SECRET=""
KEYCLOAK_ISSUER="http://keycloak.local:8080/realms/educi"

# Compte de service dédié à l'API Admin Keycloak (page /admin/users) — client
# confidentiel distinct de KEYCLOAK_CLIENT_ID ci-dessus.
KEYCLOAK_ADMIN_CLIENT_ID="educi-admin-service"
KEYCLOAK_ADMIN_CLIENT_SECRET="replace_with_your_keycloak_admin_client_secret"

# Gateway (appelé côté serveur par les routes API du frontend, depuis le
# conteneur Docker — utiliser le nom de service "gateway", pas "localhost")
GATEWAY_URL="http://gateway:8000"

# NOTE : DATABASE_URL, GROQ_API_KEY, OPENAI_API_KEY ne sont plus nécessaires
# ici — le frontend n'appelle plus Prisma ni Groq directement, il passe par
# le gateway (voir chat-service pour ces clés côté microservices).

